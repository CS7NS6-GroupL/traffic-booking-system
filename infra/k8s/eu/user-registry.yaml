apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-registry
  namespace: eu
  labels:
    app: user-registry
    region: eu
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-registry
  template:
    metadata:
      labels:
        app: user-registry
        region: eu
    spec:
      containers:
        - name: user-registry
          image: traffic/user-registry:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: traffic-config
          env:
            - name: SERVICE_NAME
              value: "user-registry"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: traffic-secrets
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-registry
  namespace: eu
spec:
  selector:
    app: user-registry
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
