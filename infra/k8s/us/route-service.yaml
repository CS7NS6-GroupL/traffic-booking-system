apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-service
  namespace: us
  labels:
    app: route-service
    region: us
spec:
  replicas: 2
  selector:
    matchLabels:
      app: route-service
  template:
    metadata:
      labels:
        app: route-service
        region: us
    spec:
      containers:
        - name: route-service
          image: traffic/route-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: traffic-config
          env:
            - name: SERVICE_NAME
              value: "route-service"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: traffic-secrets
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: route-service
  namespace: us
spec:
  selector:
    app: route-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
